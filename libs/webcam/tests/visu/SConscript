env = WengoGetQt4Environment()

libs = [
	'webcam',
	'owthread',
	'qt4',
]

include_path = [
	'.'
]

headers = [
	'webcam_test.h'
]

sources = [
	'webcam_test.cpp'
]

env.WengoAddIncludePath(include_path)
env.WengoUseLibraries(libs)

env.WengoProgram('webcam-test-visu', sources, headers)

