#include "IMContactMap.h"

IMContact * IMContactMap::findIMContact(const IMAccount & imAccount, const std::string & contactId) {
	for (const_iterator it = find(&imAccount) ; it != end() ; it++) {
		
	}
}
