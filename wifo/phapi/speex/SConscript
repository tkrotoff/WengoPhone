env = WengoGetEnvironment()

if WengoOSMacOSX():
	include_path = [
		'include'
	]
	
	env.WengoAddIncludePath(include_path)
	env.WengoAutoconf('speex', ['libspeex/.libs/libspeex.la'])

else:
	env.SConscript('libspeex/SConscript')
